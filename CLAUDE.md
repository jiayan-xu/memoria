# CLAUDE.md — jiayan-xu/memoria

Authoritative operational guide: **`AGENTS.md`** (repo root) — read it before any git or code action.

- Canonical checkout (edit & push code HERE): `C:/Users/user/.qclaw/workspace/memoria-open`, branch `main`.
- `C:/Users/user/.qclaw/workspace/memoria` is a RUNTIME MIRROR (live DB / .env / web / launchers) — NEVER
  edit code there; the running binary is built from `memoria-open`, so mirror edits won't take effect.
- Push only to `main` on the canonical GitHub remote. No secrets / absolute user paths in commits.
