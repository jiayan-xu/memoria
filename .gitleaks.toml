# Gitleaks 配置 —— agent-core / memoria 公开仓密钥扫描
# 采用 Gitleaks 默认规则集，并对已知测试占位符 / 构建产物 / 临时运行目录做豁免。
[extend]
useDefault = true

[allowlist]
description = "豁免单元测试占位密钥、构建产物、临时运行目录"
paths = [
  '''.*/target/.*''',
  '''.*/temp_run/.*''',
  '''.*/data/.*''',
  '''.*/tests/.*''',
  '''.*/test_.*\.rs''',
  '''.*_test\.rs''',
]
regexes = [
  # 单元测试用的 deadbeef 占位 admin key
  '''deadbeef[a-f0-9]*''',
  # 本地评估脚本用的固定占位 badge（非真实生产密钥）
  '''mem-admin-fixed-key-2026''',
  # eval 语料中的虚构测试串（negative 用例诱饵，非真实凭据）
  '''prod-mem-9f3a2c''',
]
