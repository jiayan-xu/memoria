---
description: 开 PR 后必须前台轮询 CI，禁止把等待甩给用户
alwaysApply: true
---

# PR 前台轮询

开 PR 或往 PR 分支再 push 之后：

1. 立刻 `gh pr checks --watch`（或 `gh run watch <id> --exit-status`）前台阻塞轮询。
2. 给时间预估（ocr+gitleaks 约 1–3 分钟；build×3 约 5–15 分钟）。
3. 绿了立刻汇报；红了立刻提取意见进入修复闭环。
4. 禁止：开完 PR 就结束对话、写「等 CI 绿了再说」、等用户来追问。
5. 不要用 `gh run list --limit 1` 当唯一数据源（会误拿并发会话的 run）。按 PR 号 / 当前分支筛。

完整流程见 `docs/PR_PROCESS.md`。
